#include "monty.h"

/**
 * f_nop - No operation, does nothing.
 * @head: stack head
 * @counter: line number
 * Return: No return
 */
void f_nop(stack_t **head, unsigned int counter)
{
    (void) counter; 
    (void) head;   
}
